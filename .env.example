PORT=3000
SESSION_SECRET=change-this-to-a-long-random-string

# Option A — single connection string (uncomment to use)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/campus_events

# Option B — individual settings (used when DATABASE_URL is not set)
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=postgres
DB_NAME=campus_events
